{
  "name": "@qala/phone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Qala phone shell: Capacitor 8 wrapper bundling the apps/web phone build (Android first, iOS later). Native glue only; no Gradle binaries in the repo.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capgo/background-geolocation": "^8.0.0",
    "@capacitor-community/text-to-speech": "^6.0.0",
    "@capacitor-community/bluetooth-le": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.6.0"
  }
}
